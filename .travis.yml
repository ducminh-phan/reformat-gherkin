language: python
dist: xenial
python:
  - "3.6"
  - "3.7"
install:
  - pip install -r requirements.txt
  - pip install -r requirements-dev.txt
script:
  - flake8
  - pytest --cov=main/
after_success:
  - coveralls
